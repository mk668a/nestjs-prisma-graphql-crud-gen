{
  "name": "usage",
  "private": true,
  "version": "1.0.0",
  "main": "src/app.js",
  "license": "MIT",
  "scripts": {
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "format": "yarn lint && prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "generate": "prisma generate && prisma migrate dev --name init && yarn format"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.5",
    "@nestjs/graphql": "^10.0.18",
    "@prisma/client": "^4.1.0",
    "graphql": "^16.5.0",
    "graphql-scalars": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "16.11.7",
    "nestjs-prisma-graphql-crud-gen": "^0.8.0",
    "prisma": "^4.0.0",
    "typescript": "4.5.2"
  }
}
